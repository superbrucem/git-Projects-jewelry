<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/styles.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        :root {
            --primary-color: #8a2be2; /* Vibrant purple */
            --primary-light: #b980ff;
            --primary-dark: #6a0dad;
            --accent-color: #ffd700; /* Gold */
            --accent-light: #ffe44d;
            --accent-dark: #ccac00;
            --text-dark: #2d3748;
            --text-light: #f7fafc;
            --bg-gradient-start: #f0e6ff;
            --bg-gradient-end: #fff9e6;
            --card-bg: #ffffff;
            --card-shadow: 0 8px 20px rgba(138, 43, 226, 0.1);
        }

        body {
            background: linear-gradient(135deg, var(--bg-gradient-start), var(--bg-gradient-end));
            color: var(--text-dark);
            display: flex;
            flex-direction: column;
            min-height: 100vh;
            width: 100%;
            margin: 0;
            padding: 0;
        }

        /* Custom styles to ensure grid layout works */
        .header-container {
            display: flex;
            justify-content: space-between;
            align-items: center;
            width: 100%;
            padding: 0 1rem;
            max-width: 1280px;
            margin: 0 auto;
        }

        .header-logo {
            display: flex;
            align-items: center;
        }

        .header-nav {
            display: flex;
        }

        .header-nav ul {
            display: flex;
            gap: 2rem;
            list-style: none;
            padding: 0;
            margin: 0;
        }

        .header-nav a {
            text-decoration: none;
            font-weight: 500;
            transition: color 0.3s ease;
            color: var(--text-light);
            font-size: 1.1rem;
        }

        .header-nav a:hover,
        .header-nav a.active {
            color: var(--accent-color);
        }

        .header-actions {
            display: flex;
            gap: 1rem;
        }

        .header-actions a {
            color: var(--text-light);
            transition: color 0.3s ease;
        }

        .header-actions a:hover {
            color: var(--accent-color);
        }

        .grid-container {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 2rem;
            width: 100%;
        }

        .grid-item {
            background: var(--card-bg);
            border-radius: 0.75rem;
            box-shadow: var(--card-shadow);
            overflow: hidden;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .grid-item:hover {
            transform: translateY(-5px);
            box-shadow: 0 12px 24px rgba(138, 43, 226, 0.15);
        }

        .testimonial-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 2rem;
            width: 100%;
        }

        .footer-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 2rem;
            width: 100%;
        }

        .hero-section {
            background: linear-gradient(135deg, var(--primary-dark), var(--primary-color));
            color: var(--text-light);
            padding: 5rem 1rem;
            text-align: center;
        }

        .cta-button {
            background-color: var(--accent-color);
            color: var(--text-dark);
            transition: all 0.3s ease;
            padding: 0.75rem 2rem;
            border-radius: 9999px;
            font-weight: bold;
            border: none;
            cursor: pointer;
            display: inline-block;
            text-decoration: none;
        }

        .cta-button:hover {
            background-color: var(--accent-light);
            transform: scale(1.05);
        }

        .section-title {
            position: relative;
            display: inline-block;
            margin-bottom: 2rem;
        }

        .section-title::after {
            content: '';
            position: absolute;
            bottom: -0.5rem;
            left: 50%;
            transform: translateX(-50%);
            width: 80px;
            height: 3px;
            background: linear-gradient(to right, var(--primary-color), var(--accent-color));
            border-radius: 3px;
        }

        .newsletter-section {
            background: linear-gradient(135deg, var(--accent-color), var(--accent-light));
            padding: 4rem 1rem;
            color: var(--text-dark);
        }

        .footer-section {
            background-color: #2d1a41;
            color: var(--text-light);
            padding: 3rem 1rem;
        }

        .container {
            max-width: 1280px;
            margin: 0 auto;
            padding: 0 1rem;
            width: 100%;
        }

        .page-content {
            flex: 1;
            padding: 2rem 0;
        }

        /* Responsive adjustments */
        @media (max-width: 1024px) {
            .grid-container, .testimonial-grid {
                grid-template-columns: repeat(2, 1fr);
            }

            .footer-grid {
                grid-template-columns: repeat(2, 1fr);
            }
        }

        @media (max-width: 768px) {
            .header-nav {
                display: none;
            }
        }

        @media (max-width: 640px) {
            .grid-container, .testimonial-grid, .footer-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <!-- Header Section -->
    <header class="shadow-lg py-6 sticky top-0 z-50 w-full" style="background: linear-gradient(rgba(0, 0, 0, 0.6), rgba(0, 0, 0, 0.6)), url('https://i.imgur.com/Ks5SXia.jpg'); background-size: cover; background-position: center;">
        <div class="header-container">
            <div class="header-logo">
                <a href="/" style="text-decoration: none; display: flex; align-items: center;">
                    <span class="text-4xl mr-3" style="color: var(--accent-color);"><i class="fas fa-gem"></i></span>
                    <h1 class="text-3xl font-bold" style="color: white; margin: 0; text-shadow: 2px 2px 4px rgba(0,0,0,0.5);">OTTAWA OPAL SHOP</h1>
                </a>
            </div>
            <nav class="header-nav">
                <ul>
                    <li><a href="/" class="<%= active === 'home' ? 'active' : '' %>">Home</a></li>
                    <li><a href="/collections" class="<%= active === 'collections' ? 'active' : '' %>">Collections</a></li>
                    <li><a href="/new-arrivals" class="<%= active === 'new-arrivals' ? 'active' : '' %>">New Arrivals</a></li>
                    <li><a href="/youtube" class="<%= active === 'youtube' ? 'active' : '' %>">YouTube</a></li>
                    <li><a href="/about" class="<%= active === 'about' ? 'active' : '' %>">About</a></li>
                    <li><a href="/contact" class="<%= active === 'contact' ? 'active' : '' %>">Contact</a></li>
                    <li><a href="/testimonials" class="<%= active === 'testimonials' ? 'active' : '' %>">Testimonials</a></li>
                </ul>
            </nav>
            <div class="header-actions">
                <a href="#" style="color: var(--text-light);"><i class="fas fa-search text-2xl"></i></a>
                <a href="#" style="color: var(--text-light);"><i class="fas fa-shopping-bag text-2xl"></i></a>
                <button class="md:hidden" style="color: var(--text-light); background: none; border: none; cursor: pointer;"><i class="fas fa-bars text-2xl"></i></button>
            </div>
        </div>
    </header>

    <!-- Main Content -->
    <main class="page-content">
